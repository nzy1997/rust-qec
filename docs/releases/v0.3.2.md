# v0.3.2 release candidate: verified Envelope decoder support

RustQEC v0.3.2 is the first candidate to publish **Supported** promises for
both `envelope-matching` and `envelope-mle`. Neither promise becomes binding
until the release assets and digest-bound post-publication verification marker
are present. MLE's promise is deliberately finite: exactly four measured
Mid-SWAP workload points, with no interpolation to other loss rates, batch
sizes, distances, round counts, or circuit families. The v0.3.1 prerelease
did not complete that verification and did not establish Supported Matching.

## Package versions

| Package | Version | Notes |
| --- | --- | --- |
| `rustqec-cli` | 0.3.2 | Only workspace package planned for publication |
| `qec-ilp-core`, `qec-code`, `rstim`, `rmatching`, `rbposd`, `rilpqec`, `rsinter` | 0.3.0 | Unchanged; not republished |

The repository tag records a tested snapshot of the 0.3 series. Every public
crate shares the 0.3 major/minor line, none is ahead of the tag, and
`rustqec-cli` matches the tag version, as required by
[`tools/release_version_policy.json`](../../tools/release_version_policy.json).

## Matching Supported scope

After publication verification, `envelope-matching` is Supported for flat
loss-visible Mid-SWAP rotated surface-code memory-Z circuits within the
executable contract in [`docs/envelope-support.json`](../envelope-support.json)
and the measured operating envelope in the retained
[resource report](../../benchmarks/atom_loss/readiness/resources/report.md).
Finite tested sizes and loss points are witnesses, not a universal circuit,
size, loss-rate, or latency guarantee.

## The MLE Supported promise

`envelope-mle` is Supported only for flat loss-visible Mid-SWAP rotated
surface-code memory-Z circuits that satisfy the executable circuit contract
and match one of these exact workload points:

| Distance | Rounds | Loss rate | Batch sizes |
| --- | --- | --- | --- |
| 3 | 2 | 0.002 | 1,024 and 16,384 |
| 3 | 1 | 0.01 | 1,024 and 16,384 |

The official Linux x86_64 and macOS arm64 archives include the required ILP
solver. Source builds must enable `--features ilp`; without it, MLE is absent
from `rustqec capabilities --format json`. The `--shot-timeout-ms` option
bounds each ILP solve phase, not compilation or total batch time. A timeout
returns `decode_timeout`, writes diagnostic statistics, and publishes no
predictions.

Every unlisted point is outside the Supported promise. In particular,
conventional Stim-annotated circuits are excluded from the MLE domain, and
the pinned conventional fixture must reject as `unsupported_circuit` before
creating predictions or statistics. A retained d=5/r=15 acceptance fixture is
a regression witness only; it does not widen the promise.

The exact domain and required correctness, resource, failure-semantics, and
installed-platform evidence are frozen in
[`docs/envelope-mle-scope.json`](../envelope-mle-scope.json).

## Hardened publication evidence

Both official native archives are built from the tagged source and tested
after installation. The release publishes
`envelope-support-evidence-v0.3.2.tar.gz` and its SHA-256 sidecar alongside the
archives. The bundle binds the release tag, source commit, support matrix, MLE
scope plan, correctness and resource reports, and both archive identities.
The readiness gate binds the exact input evidence by SHA-256. The publication
verifier repeats its completeness checks and rejects missing correctness
detail even if the outer package checksums have been recomputed. The release
must also upload `envelope-support-verification-v0.3.2.json` last, after the
published assets have been checked.

After downloading every v0.3.2 asset into one directory, verify both decoder
claims offline:

```sh
python3 tools/check_envelope_publication.py \
  --release-dir <download-dir> \
  --expect-decoder envelope-matching \
  --expect-decoder envelope-mle
```

The command ends with `PASS envelope published support` only when the two
distinct scopes and both native archive identities reconcile with the
version-bound evidence. The website remains Beta for each decoder until its
claim is independently reconciled with the final release, asset digests, and
verification marker.

## v0.3.1 disposition

The v0.3.1 tag and assets remain immutable for audit history. Its GitHub
Release is marked prerelease, its retained evidence predates the hardened
binding check, no valid verification marker exists, and no v0.3.1 CLI crate was
published. It therefore establishes no Supported Envelope matching claim.

## Compatibility

This release does not broaden the accepted circuit language or change the
stable prediction, statistics, and structured-error semantics. The frozen
surface and evolution rules remain defined by
[`docs/envelope-compatibility-policy.md`](../envelope-compatibility-policy.md).
